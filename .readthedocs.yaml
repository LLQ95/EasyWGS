# Read the Docs configuration (v2); documentation source is MkDocs Material
version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"

mkdocs:
  configuration: mkdocs.yml

python:
  install:
    - requirements: requirements-doc.txt
